{
  "name": "helveticoin",
  "version": "1.0.0",
  "description": "HelvetiCoin (HCHF) — Swiss Stablecoin Professional Blueprint",
  "private": true,
  "workspaces": [
    "ui",
    "contracts"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=ui",
    "build": "npm run build --workspace=ui && npm run compile --workspace=contracts",
    "test": "npm run test --workspace=contracts && npm run test --workspace=ui",
    "lint": "npm run lint --workspace=ui && npm run lint --workspace=contracts",
    "format": "prettier --write .",
    "contracts:compile": "npm run compile --workspace=contracts",
    "contracts:test": "npm run test --workspace=contracts",
    "contracts:deploy": "npm run deploy --workspace=contracts",
    "ui:dev": "npm run dev --workspace=ui",
    "ui:build": "npm run build --workspace=ui",
    "ui:test": "npm run test --workspace=ui",
    "foundry:test": "cd contracts && forge test",
    "foundry:build": "cd contracts && forge build"
  },
  "keywords": [
    "helveticoin",
    "hchf",
    "stablecoin",
    "ethereum",
    "tron",
    "defi",
    "switzerland",
    "crypto"
  ],
  "author": "HelvetiCoin Team",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}